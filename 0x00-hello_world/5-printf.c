#include <stdio.h>

/**
 * main - prints a string
 *
 * Returns: 0( after the code runs succesfully)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
